services:
  - type: web
    name: genius-vendas-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: DATABASE_URL
        fromDatabase:
          name: genius-vendas-db
          property: connectionString

databases:
  - name: genius-vendas-db
    databaseName: genius_vendas
    user: genius_vendas
